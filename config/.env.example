# 环境变量配置示例
# 复制此文件为 .env 并填写真实值

# 数据库连接（PostgreSQL）
# 格式：postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fund_screener

# ========== 推送渠道配置（至少配置一个） ==========

# Server酱（推送到个人微信）
# 获取地址：https://sct.ftqq.com/
SERVER_CHAN_KEY=your_sckey_here

# 企业微信机器人（推送到企业微信群）
# 获取方式：在企业微信群中添加机器人，复制Webhook地址
WECOM_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key_here

# ========== 邮件推送配置 ==========
# 支持Gmail、QQ邮箱、163邮箱等SMTP服务

# SMTP服务器地址
SMTP_HOST=smtp.gmail.com

# SMTP端口（TLS: 587, SSL: 465）
SMTP_PORT=587

# 发件人邮箱地址
SMTP_USER=your_email@gmail.com

# SMTP密码/应用专用密码
# Gmail: 需要生成应用专用密码（账户设置 → 安全 → 两步验证 → 应用专用密码）
# QQ邮箱: 使用授权码（设置 → 账户 → POP3/SMTP服务 → 生成授权码）
# 163邮箱: 使用授权码
SMTP_PASSWORD=your_app_password_here

# 收件人邮箱（多个用逗号分隔）
EMAIL_RECEIVER=receiver@example.com