# 基金筛选系统 - Python依赖
# 使用 uv 安装: uv pip install -r requirements.txt

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.1.0
# psycopg2-binary>=2.9.0  # PostgreSQL (保留但未启用)
alembic>=1.12.0

# 数据抓取
akshare  # 始终使用最新版
requests>=2.31.0

# 定时任务
apscheduler>=3.10.0

# 其他工具
python-dotenv>=1.0.0
loguru>=0.7.0

# 开发工具（可选）
pytest>=7.4.0
black>=23.0.0